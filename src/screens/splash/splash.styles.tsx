import {
    StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
    splashStyle: {
        flex:1,
        backgroundColor: '#2F4F4F',
        justifyContent:'center',
        alignItems:'center'
    }
})