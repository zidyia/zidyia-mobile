import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        margin: 10
    },
    textInput: {
        borderWidth: 1.5,
        borderColor:'#FFF',
        height: 50,
        borderRadius: 5,
        fontSize: 18,
        color: '#FFF'
    }
});